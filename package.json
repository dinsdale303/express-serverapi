{
  "name": "express-serverapi",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon server",
    "build": "cd client && yarn install && yarn build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.4",
    
  }
}
